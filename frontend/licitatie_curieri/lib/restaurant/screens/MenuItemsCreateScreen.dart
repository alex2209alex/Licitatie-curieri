import 'package:flutter/material.dart';

class MenuItemsCreateScreen extends StatelessWidget {
  const MenuItemsCreateScreen({Key? key, required restaurantId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
